(()=>{
  "use strict";
  // Compatibility shim for cached admin pages. Full-page reloads after staff or
  // service saves restarted every initialization scan, so they are disabled.
  window.__ANAUTS_OPERATIONS_REFRESH_FIX__=true;
})();
